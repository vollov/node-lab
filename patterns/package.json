{
	"name": "pattern-demo",
	"description": "Node JS design pattern",
	"version": "0.0.1",
	"private": true,
	"scripts": {
		"start": "supervisor pattern.3000.js",
		"test": "mocha --ui bdd -R spec test/**/*.test.js"
	},
	"dependencies": {
		"express": "3.0.0"
	}
}
